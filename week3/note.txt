Cookie:
- La mot storage cua browser
- Cac thuoc tinh:
 -name, value, time to live, domain, oath, httpOnly(tai client khong the xem duoc no), Secure

 Cookie create nhu the nao

 1. JS ENginge trinh duyet cung cao ham de minh tao
 2. Server co the goi header: Set-Cookie: a=b
 Khi nao client no goi cookie len server:
 -Bat ky request nao deu goi cookie.

 Session:
 - server kiem tra session_id, tạo mới gửi về client.
 - Tạo ra một cái storage riêng cho sesion đó. Bỏ vào tủ đồ giống như identity của ông ( Redis/MySql)
 - Thường sẽ lưu trữ ở cookie.

 JWT: claim.Subject: identity

 Nginx vs Apache : apache support configure web server easily (should use it when provide a hosting service)